# Normalize every detected text file to LF in both the repository and working
# tree.  This keeps a Windows checkout safe to copy to Linux while Git leaves
# binary files untouched.
* text=auto eol=lf
