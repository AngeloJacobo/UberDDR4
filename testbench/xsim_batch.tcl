# Headless XSim execution for scripted regressions.
#
# `run all` advances until $finish. Plain `quit` is accepted by both Linux
# and Windows XSim; `quit -force` is not valid in Vivado 2023.1 on Linux.
run all
quit
